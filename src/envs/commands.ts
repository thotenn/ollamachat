export const COMMANDS = {
  CORS_PROXY: 'npm run cors-proxy',
} as const;

export const API_HEADERS = {
  CONTENT_TYPE: 'application/json',
  ANTHROPIC_VERSION: 'anthropic-version',
  X_API_KEY: 'x-api-key',
  AUTHORIZATION: 'Authorization',
} as const;